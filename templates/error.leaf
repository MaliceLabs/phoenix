doctype

html
	head
		meta charset: "utf-8"

		title "Internal Server Error – Fur Affinity"

	body
		h1
			"#{data.name}"

			if data.message
				": #{data.message}"

		if data.stack
			pre "#{data.stack}"
